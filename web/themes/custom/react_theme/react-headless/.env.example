# Copy this file to .env and adjust for your environment.
# Only used when running the SPA standalone with `npm run dev`
# (i.e. NOT embedded in the Drupal theme, where react_theme.theme
# supplies the base URL / CSRF token via window.drupalSettings instead).
VITE_DRUPAL_API_URL=http://drupal-headless.site
